import math
x = input("Nhap gia tri x: ")
x = float(x)
f - x = math.log(4,x) + math.log(2,x)
print(f"Gia tri can tim f(x) = {f - x: 2f}")

def f(x):
    numerator = -x + math.sqrt(x**2 + 4)
    denominator = (x**4 + 1)**(1/7)
    return numerator / denominator

# Nhập giá trị x từ người dùng
x = float(input("Nhập giá trị của x: "))
result = f(x)

# In kết quả đã làm tròn đến 2 chữ số thập phân
print(f"Giá trị của f(x) là: {result:.2f}")

import math
x = input("Nhap gia tri x: ")
x = float(x)
f_x == math.log(4,x) + math.log(2,x)
print(f"Gia tri can tim f(x) = {f - x: 2f}")